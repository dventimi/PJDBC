#!/usr/bin/bsh
    try {
	source("init.bsh");
	assert(true);
	assertTrue(true);
	assertFalse(false);
	assertNull(null);
	assertNotNull("");
	assertEquals("a", "a");
	assertNotEquals("a", "b");
	assertSame("a", "a");
	assertNotSame("a", "b");
    } catch (e) {System.exit(1);}
